# Python Development Skill
When writing Python scripts or web services:
1. Ensure virtual environments or clean dependency usage.
2. Structure entry points with `if __name__ == '__main__':`.
3. Handle exceptions cleanly and format logging with timestamps.
