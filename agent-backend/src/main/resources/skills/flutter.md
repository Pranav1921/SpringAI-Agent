# Flutter Engineering Skill
When building Flutter apps:
- Create `pubspec.yaml` with required dependencies (e.g. `flutter`, `cupertino_icons`, `provider`).
- Create `lib/main.dart` with a clean Material 3 design, StatefulWidgets, responsive layout, and dark mode theme.
- Support `web/index.html` for web rendering.
